<div class="text-center">
  <h1  style="color:#91a0d8;"><i class="fas fa-calendar-check"></i> 以下の内容で予約してよろしいですか？ </h1>
</div>

<div class="row">
  <div class="col-sm-6 offset-sm-3">
    
   <table class="mt-4 table table-striped table-bordered justify-content-center">
    <thead>
      <tr>
        <th colspan=2 style='text-align: center;  background-color: #91a0d8; color: #495152 ;'>予約内容</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td >場所</td>
        <td style='text-align: left;'> 
        <%= @allocation.room.facility.name %>  <%= @allocation.room.name %>
        </td>
      </tr>
      <tr>
        <td>日時</td>
        <td style='text-align: left;'><%= @allocation.date.month %>月 <%= @allocation.date.day %>日
              <%= @allocation.timeslot.time_start.hour %>:00 ~ <%= @allocation.timeslot.time_finish.hour %>:00</td>
      </tr>
      <tr>
        <td>料金</td>
        <td style='text-align: left;'><%= @allocation.room.price %> 円</td>
      </tr>
    </tbody>
  </table>
      

    <%= form_with(model: @reservation, local: true) do |f| %>
      <%= render 'layouts/error_messages', model: f.object %>
      <div class="form-group">
        <%= f.hidden_field :allocation_id, class: 'form-control', value: params[:allocation_id] , readonly: true %>
      </div>
      
      <div class="form-group">
        <%= f.hidden_field :user_id, class: 'form-control', value: @user.id , readonly: true %>
      </div>
      
      <div class="form-group">
        <%= f.hidden_field :status, class: 'form-control', value: '予約' , readonly: true  %>
      </div> 
    
      
      
      <div id='edit'>
      <%= link_to "戻る", :back, class: 'btn' %>
      <% if @judge %>
        <button class='btn' disabled> 既に予約しています</button>
      <% else %>
        <%= f.submit 'OK', class: 'btn' %>
      <% end %>
      </div>
    <% end %>
  </div>
</div>